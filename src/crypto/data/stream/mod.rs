use std::collections::HashMap;

#[derive(Debug)]
pub struct StreamBuilderCrypto {
    // pub channels: HashMap<ExchangeId, Channel<MarketStreamResult<InstrumentKey, Kind::Event>>>,
    // pub futures: Vec<SubscribeFuture>,
}
